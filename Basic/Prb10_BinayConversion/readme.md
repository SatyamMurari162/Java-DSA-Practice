P10. Decimal to Binary Conversion

Problem:
Convert the decimal number 156 to its binary representation without using a built-in conversion function.

Approach:
Repeatedly divide the number by 2, recording the remainder (0 or 1) at each step — these remainders, read in
REVERSE order, form the binary representation.