P1. Reverse a String Without Built-in Reverse

Problem: Reverse the string 'ACCENTURE' without using slicing [::-1] or a built-in reverse function.
oi
Approach:
Use two pointers starting at opposite ends of a character list, swapping characters and moving both pointers toward the
centre until they meet.