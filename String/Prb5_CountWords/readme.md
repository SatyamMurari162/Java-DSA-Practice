Q. Count the Number of Words in a Sentence

Problem: Count how many words are in the sentence 'The quick brown fox jumps over the lazy dog'.

Approach:
Use split() (which splits on any whitespace and handles multiple spaces gracefully) and count the resulting list's length