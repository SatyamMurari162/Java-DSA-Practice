P36. Print an Inverted Right-Angled Triangle

Problem: Print an inverted star triangle with 5 rows (starting wide, narrowing to a point).

Approach:
Run the outer loop from n DOWN to 1 instead of 1 up to n — everything else about the star-printing logic stays identical
to the standard triangle.